$( function() {
    $( "#accordion" ).accordion();
} );

// const questionHeading = document.querySelectorAll('.questions__heading')
// const icon = document.querySelectorAll('.questions__heading::after')

// questionHeading.forEach(e => {
//     if (e.classList.contains('ui-accordion-icons')) {
//         icon.classList.add('rotate')
//     }
// });